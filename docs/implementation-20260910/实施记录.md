# 2026-09-10 最终方案实施记录

依据：用户指定《整体修改方案-最终复审稿.md》（2026-09-10），全部 17 项及引用的四张已选样张、原版表情 HTML/生产主图集与左右探头图集、原始六块红色 Logo 已核对。附件内“未启动实施”是原方案记录，当前用户已明确授权实施。

## 开始时的状态

基线 HEAD：c348136b58f1b386e069b1f0423669d304f732c3，版本 1.0.52。
未提交候选：7 个跟踪文件和 notification-delivery.ts / test；未跟踪的 PPT、手册和 output 素材不作为正式成果采用，也不覆盖。
原始差异和候选逐文件备份：artifacts/implementation-20260910/baseline/（candidate.patch、manifest.json、源码快照）。

候选复审：
- 布局回执与原生显示串联的方向可保留；既有实现无限重试、5 秒长期探测、ready 无限补报不符合最终要求，必须替换。
- 移除 out-in 卡片切换可避免隐藏 WebView 等待旧卡退场；保留单层卡片及消息身份 key，拖动不能重建卡片。
- 原生新增的可见性查询仅服务长期轮询，本次撤回该候选查询 API。
- 候选招手只判断显示，仍无限循环且压过拖动，必须改为真实动画两轮并让直接拖动优先。
- 两份候选源码断言测试需随实际合同修改，不作为候选正确性的依据。

## 分批实施

1. 提醒送达、失败终止、完整请求截止时间和旧账号结果隔离。
2. 原生工作区/卡片尺寸、输入外点及输入法、单行自动多行、固定按钮及正文滚动。
3. 30 分钟内有效消息补入，已有快照全部已读保护，业务类型优先分类，已确认卡片视觉。
4. 881444ae 外观适配原版表情与完整左右动作，原始红 Logo；生产素材单独审查。
5. 相关 Windows 与管理员静默升级门禁，独立 MSI、原格式 ZIP、脚本、说明、校验和签名状态。

用户补充保护项：现有 30 分钟未操作后桌面自动消失保留，不等于后台已读/任务完成，补入及重连不延长有效期。

## 发布阻断项与证据边界

白块尚无问题机 HWND/绘制区域归属证据。正常运行环境不关闭 GPU、不主动降级渲染、不加入循环闪现恢复。
新形象生产图集、卡片原生显示与拖动、Win10/11 问题机和正常机对照、静默升级均须按实际完成情况记录。浏览器截图、本地构建或静态素材不能替代 Windows 验收。
版本仍为 1.0.52；目前没有新正式发布包。

## 2026-09-11 代码与验证进展

分支：`codex/desktop-experience-20260910`。已提交网络/补入一批、显示/交互一批、Windows 候选验证一批。原有未跟踪的 PPT、手册、output 和脚本保持原状。

已实现：
- 提醒先取得布局回执再显示，首次加最多两次补救，总时限 10 秒；取消、失败终止、旧回执与账号隔离。成功后的内容更新保留当前卡片。
- 完整 HTTP 请求（含正文读取）和工作台交接各有 12 秒上限；写请求不自动重发，未知交接结果保留草稿。
- 首次补入限 5 页、100 条、10 秒，原 30 分钟有效期不延长；按业务类型统一名称、图标和会议蓝/待办绿。
- 有内容外点、空闲与拖动保留面板；Esc 保留草稿，输入法优先；多行展开先调原生窗口，按钮和输入滚动区域分开。
- 长标题、正文和错误可读到末尾，按钮留在可见区域；小工作区先上/下/侧方安置，再限制阅读区，避免卡片覆盖机器人。
- 招手以实际显示及真实动画循环计数；新消息重置动画相位，重复消息不重播；直接拖动结束本次招手，减少动态效果设置优先。
- 任务操作返回时再次确认原任务仍属于当前队列，防止旧账号结果移除新账号同 ID 任务或写入错误提示；过期任务操作不抢回焦点。

必要验证：
- 本地相关服务、提醒、会话、输入/窗口与动作保护检查通过；最新任务账号隔离及相关合同 34 项通过，`build:intranet` 与 `git diff --check` 通过。
- 实际 Vue 浏览器验证：单行 78 → 多行 178 → 单行 78；发送按钮独立；Esc 草稿和输入法优先；160px 视口的完整长提醒、错误、按钮及最后一行可访问，队列数量更新不重置阅读位置。
- Windows run **34548178777** 对应提交 **9230feb**，全部通过：相关前端检查、原生 geometry/panel/notification/outside-focus、rustfmt、clippy 无警告、内网可执行程序构建、真实 HWND/鼠标视觉检查。
- Windows 截图及报告：`artifacts/implementation-20260910/windows-run-34548178777/`。该环境为 CI Windows、96 DPI；包含首次点击、双击工作台路径、右键上下放置/外点关闭、6 次通知隐藏后的点击穿透、右侧半头悬停恢复、隐藏后保持隐藏。不能将它表述为问题机或 Win10/11 全部场景验收，也未覆盖管理员 MSI 升级。
- 后补的任务结果隔离仅改前端，单独完成任务测试与构建；不将此前 Windows run 冒充该后补提交的完整验证。

## 新机器人素材的实际状态

生产主图集、运行图集、左右探头图集均未替换。
前三次生成结果缺少真实 alpha 或改变轮廓，未采用。用户回复继续后，试制程序化合成：直接从 881444ae 提取透明轮廓、从原生产帧提取表情像素，原始 Logo 截图按其 ICC 色彩配置转换到 sRGB 后提取六块图形。

非生产关键帧与 100/125/150/175/200% 浅深底对照位于 `artifacts/implementation-20260910/robot-keyframes/`，脚本为 `tmp/implementation-20260910/robot-keyframe.py`。这解决了真实透明与原版表情来源问题，但还没有完整的动作图集、连续动作锚点与左右进入/停留/返回验证，不能作为最终素材交付。

额外的四姿态生成研究（35ff0bca）也仅是中间参考，尚未证明完整动作与原版探头姿态一致，不纳入生产。

剩余发布门禁：完整新素材达到要求；白块原问题机归属/复现与正常 Win10/11 对照；带消息卡片拖动、代表缩放及系统恢复；最终提交的管理员/SYSTEM 静默升级；正式 MSI、ZIP 和校验。当前明确不声称整体方案或发布已经完成。

## 2026-09-11 1.0.53 管理员推送包验收

以下更新上文阶段性状态。本次按稳定性优先交付功能改进包，已向用户明确说明：完整新外观尚未达到生产要求，因此保留原版完整图集；不能将这个包称为全部最终方案已完成。白块原问题机及公司 Win10/Win11 代表客户端验证仍未完成。

发布分支：`codex/desktop-experience-1.0.53`。构建提交：`3c860d933dc4d4627789cb4f4f461e55c6a57e55`。
最终成功的 Windows Actions：`34574308192`。MSI、ZIP、部署脚本、发布说明和完整 QA-EVIDENCE 已上传。本节文档更新不改变已验收程序。

额外修复了 30 分钟过期与异步操作交错的边界：旧消息过期后，新卡片可正常操作；旧请求的失败提示和 finally 清理不覆盖新卡片的状态；全部已读按原消息快照隔离。直接提取实际处理函数的延迟请求验证通过。

最终提交的必要验证：
- 15 个相关前端测试文件共 122 项、消息异步生命周期行为检查、内网前端构建通过；原生 geometry、panel、notification、outside_focus、context_menu 范围检查和 clippy 通过，未执行无关全量测试。
- 已安装并运行的 1.0.52 通过管理员 `/qn` 升级为 1.0.53；程序路径稳定，旧进程关闭，产品注册唯一。尾斜杠协议回调、单实例、会话提交、消息订阅、真实卡片显示和重启恢复通过。
- 管理员默认启动进入交互会话，HKLM Run 保底存在；升级后卸载及最终卸载完成注册、启动项、程序和进程清理；历史指定诊断文件清理不删除无关文件。
- 真实鼠标拖动的 12 个采样保持同一机器人及消息卡片 HWND；第 2 步起纵向跟随，第 9 步起横向离开工作区边界约束，7 个采样满足持续有效移动阈值；拖动中及松手后没有卡片覆盖机器人。
- Windows 96 DPI 画面人工复核：消息完整、按钮可见；29 个动画采样、8 个不同帧，透明边缘像素检查通过；右键菜单上下放置、外点关闭、半头悬停恢复、6 次通知隐藏后点击穿透、主动隐藏后保持隐藏通过。这是 CI Windows Server 2025 的证据，不代表 Win10/Win11 全部显卡、缩放或白块原问题机已验收。

失败构建 `34573244726` 未交付。其拖动脚本从左上角继续拖出工作区，把卡片边界保护误判为未跟随，且 CI 默认为轮廓拖动。随后只修正验收路径：向工作区内部拖动，临时启用完整窗口拖动并在 finally 恢复原设置，再增加拖动过程持续跟随检查；没有为通过检查改动产品设置或降低标准。

最终交付文件位于 `artifacts/release-v1.0.53-run-34574308192/`；下载后的完整性、MSI/ZIP 校验值及安装后程序一致性以该目录的 `delivery-verification.json` 为准。签名状态仍为内网未签名包；管理员部署命令及限制见 `docs/releases/v1.0.53.md` 和包内说明。

下载核验已全部通过：`unzip -t` 无错误；独立 MSI 与 ZIP 内 MSI 字节一致；SHA256.txt 按 Windows CRLF 换行读取后校验通过。MSI SHA-256：`698537a7aafcaefab047a5443612f608cdc24abced09c18a5b3ad7958bd90e92`。ZIP SHA-256：`b3dca1fbff3f3679526257caa4b158a6b96de95f3fb283a25354bd8684678495`。

主程序的两种校验值已查明并验证：直接提取 MSI 的 CAB 主程序得到 `d6ec0198a963cdf85f9beac2be400bd2d2ab4495f86beae0986adcd305f9422a`，与安装后程序完全相同。包内 APPLICATION-SHA256.txt 记录的是打包前程序值 `3e9596e9b5a62a37b7c4c835e998a6ca03723bcd4225b57d0124690fe69d7e99`；将唯一 MSI 类型标记还原为 UNK 后恰好匹配，没有其他字节差异。这是 [Tauri CLI 2.11.2 的打包与还原行为](https://github.com/tauri-apps/tauri/blob/tauri-cli-v2.11.2/crates/tauri-bundler/src/bundle.rs)，不是错装其他程序。已在交付核验记录中分别记录这两个含义，不修改通过 Windows 验收的 MSI 或 ZIP。
